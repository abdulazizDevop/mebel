# Compose-level secrets (read by `docker compose`). Keep separately from
# backend/.env so we can rotate Postgres creds without touching app config.
POSTGRES_USER=mebel
POSTGRES_PASSWORD=CHANGE_ME_TO_A_LONG_RANDOM_STRING
POSTGRES_DB=mebel
